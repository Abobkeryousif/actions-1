name:  generate assci code
on:  push

jobs:
  ascii-job:
    runs-on: ubuntu-22.04
    steps:
        - name: checkout repo 
          uses:  actions/checkout@v5

        - name: execute shell script
          run: ./ascii-script.sh


        
    